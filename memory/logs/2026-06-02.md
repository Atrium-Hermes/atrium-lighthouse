# 2026-06-02

## Atrium scout
- Ran `atrium-scout` (no `${var}` focus → scouted all open loops).
- Memory near-bootstrap; built a 4-item need list (PR review, digest, changelog, recurring jobs).
- Queried the public indexer: marketplace has **15 skills live**, ~11 total invocations across all.
- Best match: **github-code-review** ($0.005/call, developer-tools) for the mandated PR-review step.
- **Rented nothing** — no PR open to review, skill has 0 proven invocations, and the `atrium` CLI/wallet key aren't reachable in this sandbox. Report-only.
- Saved full report to `memory/atrium/scout-2026-06-02.md`. Sent summary via `./notify`.

## Atrium scout (run 2)
- Re-ran `atrium-scout`. Installed the atrium CLI (`npm i -g @atrium-hermes/cli`) — works out of the box (Base registry + USDC baked in).
- Ran `atrium balance` → **`ATRIUM_PRIVATE_KEY not set`**. Confirmed `aeon.yml` passes the key to no step → renting is structurally impossible today.
- Top pick: **generate-playwright-tests** ($0.1, **6 invocations** — most-proven on the market, under the $0.5 cap). Queued at `.pending-atrium/generate-playwright-tests.json`.
- Opened PR `scout/wire-atrium-invoke`: adds `scripts/postprocess-atrium.sh` (secure spend, key kept out of the agent step), wires `ATRIUM_PRIVATE_KEY` into the post-process step, gitignores `.pending-atrium/`, updates SKILL.md step 4.
- **Operator action:** set the `ATRIUM_PRIVATE_KEY` secret + merge → next run rents automatically. Report-only this run.
- Report: `memory/atrium/scout-2026-06-02-b.md`.

## Atrium scout (run 3)
- Re-ran `atrium-scout`. Marketplace unchanged: **15 skills, 12 total invocations**.
- **Key now wired:** `aeon.yml:807` passes `ATRIUM_PRIVATE_KEY` into the post-process step (commit `c25252b`, run-2 PR merged). Renting is now structurally possible.
- Top pick unchanged: **generate-playwright-tests** ($0.1, **6 inv** — most-proven on the market, under the $0.5 cap). The run-2 pending request (`.pending-atrium/generate-playwright-tests.json`) still matches and is not yet rented, so I left it in place (one-pending-per-run cap satisfied) rather than duplicating.
- This is the first run where post-process can actually invoke. Expect `memory/atrium/rented/generate-playwright-tests.md` + a `.done` marker if the secret is set & wallet funded; otherwise post-process no-ops and we stay report-only.
- Report: `memory/atrium/scout-2026-06-02-c.md`.

## Atrium scout (run 4)
- Re-ran `atrium-scout`. Marketplace unchanged: **15 skills, 12 total invocations**.
- Post-process bug fixed in commit `23ecb65`: no longer skips the rental on a `atrium balance` CLI quirk (invoke enforces funds on-chain). First run where the queued request can actually settle.
- Top pick unchanged: **generate-playwright-tests** ($0.10, **6 inv** — most-proven on market, under the $0.5 cap). `crypto` has 2 inv but is $5 (10× over cap) → excluded.
- The run-2/3 pending request (`.pending-atrium/generate-playwright-tests.json`) still matches and is not yet rented (no `.done`, `rented/` empty), so I left it in place rather than duplicate. One-pending-per-run cap satisfied.
- Report: `memory/atrium/scout-2026-06-02-d.md`. Added `.atrium_skills.json` to `.gitignore` (sandbox blocked `rm` of the temp file).
