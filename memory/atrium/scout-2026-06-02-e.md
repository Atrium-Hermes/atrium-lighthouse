# Atrium scout — 2026-06-02 (run 5)

## Mode
Standing-loop scout (no `${var}` focus). Memory near-bootstrap, so per
`scout-config.md` the active loop is "acquire one new useful skill."

## Marketplace snapshot
- Indexer reachable (curl OK this run). **15 skills live, unchanged from runs 1–4.**
- Total proven usage is still thin: only 5 skills have any invocations.

| Rank | Skill | Price (USDC) | Invocations | Category | Under cap? |
|------|-------|--------------|-------------|----------|-----------|
| 1 | **generate-playwright-tests** | 0.10 | **6** | development | ✅ |
| 2 | crypto | 5.00 | 2 | — | ❌ 10× over cap |
| 3 | bankr-token-launch | 0.01 | 1 | onchain-finance | ✅ |
| 4 | premium-prompt | 0.01 | 1 | prompts | ✅ |
| 5 | openclaude-loop | 0.002 | 1 | agent-runtime | ✅ |
| 6 | (unnamed) | 0.10 | 1 | — | ✅ (no metadata) |
| 7–15 | base-token-swap, onchain-token-intel, agent-wallet-ops, pdf-toolkit, mcp-server-builder, webapp-testing, github-code-review, jupyter-live-kernel, cheapest-base-swap-with-slippage | 0.003–0.006 | 0 | various | ✅ |

## Pick (unchanged)
**generate-playwright-tests** — `0xa4486c22d45af49913ebca595234e8897e8f22bbb6c7e348fb1e424de9572f09`
- $0.10/call, **6 invocations** (most-proven skill on the market), $0.585 earned.
- Comfortably under the $0.50 cap.
- Unblocks: generate Playwright E2E tests for the dashboard or any web target from a URL + a short spec.
- Tie-break logic moot — it leads on invocations outright; `crypto` (2 inv) is the only
  other multi-invocation skill and is 10× over the cap.

## Rental status — report-only again
- Pending request already queued at `.pending-atrium/generate-playwright-tests.json`
  (from run 2; skillId matches the current top pick, not yet rented).
- `memory/atrium/rented/` is **empty** and there is **no `.done` marker** after 4 prior
  runs where post-process could have settled → `ATRIUM_PRIVATE_KEY` is almost certainly
  still unset. Without the key, `scripts/postprocess-atrium.sh` no-ops by design.
- Left the existing pending request in place (one-pending-per-run cap satisfied); did
  not duplicate it.

## Operator action to go live
Set the `ATRIUM_PRIVATE_KEY` repo secret (wallet funded with a small USDC float on
Base). The wiring is already merged: `aeon.yml` passes the key into the post-process
step, and `postprocess-atrium.sh` enforces the cap + dedupe and runs `atrium invoke`.
Next scheduled run after the secret is set should settle the queued rental
automatically and write `memory/atrium/rented/generate-playwright-tests.md`.
