# Atrium scout — 2026-06-05 (run b)

**Mode:** standing-goal (no `${var}` focus) — acquire one new useful skill.
**Indexer:** reachable (curl OK). **Marketplace: 42 listed skills, ~43 total invocations.**

## Settlement check
- **`api-health-check` settled** ($0.003, tx `0x804e06b371130f4c501bc70ed6bdb6b97633b8028a8f0c45396f9b6b6b1bf25e`).
  Queued in the 06-05 morning run; body on disk at `memory/atrium/rented/api-health-check.md`;
  indexer shows **4 inv** (our call landed). Row was **missing from MEMORY.md** → reconciled
  (added row + settlement note). Unblocks the heartbeat / skill-health monitoring loops.

## Candidate ranking (non-rented, under $0.5 cap)
Already rented (skipped): generate-playwright-tests (7 inv), api-health-check (4),
bankr-token-launch (3), schema-extract (3), semver-decide (3), log-analyzer (2),
git-changelog (2), openclaude-loop (2).

Top non-rented invocation tier (**3 inv**, the leader below the rented set):

| skill | price | inv | attest | category | note |
|-------|-------|-----|--------|----------|------|
| **api-doc-generator** | $0.008 | 3 | — | developer-tools/documentation | **PICK** — new, non-redundant capability |
| smart-extract | $0.008 | 3 | — | data-processing | overlaps already-rented `schema-extract` |

`crypto` (2 inv, **$5**) excluded — 10× over the $0.5 cap. Every other non-rented skill is ≤2 inv;
attested skills (json-repair, web-extract, erc20-approval-guard, sql-guard — each 1 stake) are all
≤1 inv, so they lose the invocation tiebreak to the 3-inv pair.

## Decision
**Pick: `api-doc-generator`** ($0.008, 3 inv, no attestations,
skillId `0x3dae2dd27278707c1a28693b3cf15a067aa329ea5c1d313fd2a481b82f527864`).

Tie at the top tier was `smart-extract` vs `api-doc-generator` — same inv (3), same price ($0.008),
neither attested. Broke the tie on **fit / non-redundancy**: `smart-extract` (free text → typed JSON)
heavily overlaps the already-rented `schema-extract`, while `api-doc-generator` adds a genuinely new
capability. Fits a real loop — this agent writes code, builds skills, and opens PRs continuously, so
auto-generating API docs (endpoints, request/response examples, auth guides, error catalogs, SDK
snippets) from source / OpenAPI specs is directly useful and pairs with the existing
git-changelog + semver-decide release-automation pipeline.

$0.008 ≤ $0.5 cap, not already rented → queued at `.pending-atrium/api-doc-generator.json`.
`scripts/postprocess-atrium.sh` (with the wallet key) will enforce the cap again and execute the spend.

## Future candidates (once any picks up an invocation)
- Attested but 0–1 inv: `json-repair`, `web-extract`, `erc20-approval-guard`, `sql-guard`.
- High-fit unattested 0-inv: `config-validator`, `error-root-cause-analyzer`, `regex-wizard`,
  `universal-test-generator`, `code-security-audit`, `dockerfile-optimizer`.
