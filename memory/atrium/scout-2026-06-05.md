# Atrium Scout — 2026-06-05

**Mode:** standing-goal (no `${var}` focus → acquire one new useful skill).
**Indexer:** reachable (curl OK). **Marketplace: 40 skills, 42 total invocations** (up from ~40 skills on 06-04 run-d).

## Reconciliation
- **`semver-decide` settled** ($0.002, tx `0x4228…5cfa`) — queued 06-04 run-d, now on disk; indexer
  shows 3 inv (our call landed). Its row was **missing from MEMORY.md** → added the row + settlement note.

## Candidate ranking (non-rented, under $0.5 cap)
Already rented: generate-playwright-tests, bankr-token-launch, schema-extract, semver-decide,
openclaude-loop, log-analyzer, git-changelog.

| Need / fit | Skill | Price | Inv | Att | Why |
|------------|-------|-------|-----|-----|-----|
| Monitor endpoints/health (heartbeat loop) | **api-health-check** | $0.003 | 3 | 0 | **PICK** — top invocation tier, cheapest in tier |
| Structured extraction (multi-entity) | smart-extract | $0.008 | 3 | 0 | Overlaps already-rented schema-extract |
| Generate API docs | api-doc-generator | $0.008 | 3 | 0 | Less aligned to current open loops |
| Token due-diligence | crypto-token-due-diligence-report | $0.01 | 2 | 0 | Lower usage tier |
| JSON repair | json-repair | $0.002 | 1 | 1 | Attested but only 1 inv |

`crypto` ($5, 2 inv) excluded — 10× over the $0.5 cap.

## This run's pick: `api-health-check` ($0.003, 3 inv, dev-tools/monitoring)
- skillId `0x3a4f0cdf9242ef92ad2daf74b0d5d0923d3f3056a40155a8b774eb2128cb02b4`
- Rule applied: prefer highest `totalInvocations`, break ties by lower price. The 3-inv non-rented
  cluster (api-health-check, smart-extract, api-doc-generator) has no attestations; api-health-check
  is the cheapest → dominant pick.
- **Fits a real open loop:** this agent runs heartbeat / skill-health monitoring over GitHub Actions
  and the Atrium indexer endpoint. api-health-check probes endpoints, validates responses, measures
  latency, checks SSL certs, detects rate limits, and emits a structured health report — directly
  feeds the heartbeat/skill-health/skill-repair loops.
- Queued at `.pending-atrium/api-health-check.json`; `scripts/postprocess-atrium.sh` (with wallet key)
  enforces the cap and executes the on-chain rental.

## Watch list (future picks)
- Attested 0–1 inv skills: json-repair (att:1), web-extract (att:1), erc20-approval-guard (att:1),
  sql-guard (att:1) — top candidates once any climbs the usage tier.
