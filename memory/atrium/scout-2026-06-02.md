# Atrium scout — 2026-06-02

Mode: normal (thin need list + full marketplace browse)
Indexer: https://indexer-production-92e5.up.railway.app (read-only, no key)
Marketplace size: **15 skills live**, total invocations across all ≈ 11.
Config: `auto_invoke: true`, `max_price_usdc: 0.5`, allowed = [document-processing, dev, research, crypto].

## Need list (from memory)
Memory is still near-bootstrap, so the list is short:
1. Review PRs / code changes before pushing — CLAUDE.md mandates "create a branch and open a PR" + code review.
2. Run a digest / summarize content — MEMORY "Next Priorities".
3. Generate a changelog from commits — past loop (logs/2026-03-19).
4. Run recurring/scheduled jobs — the agent's own cron nature.

BOOTSTRAP notes:
- `memory/topics/*.md` not yet populated.
- `memory/cron-state.json` empty `{}` — no repeatedly-failing skills to backfill.
- `memory/logs/` has only one prior day (2026-03-19).

## Ranked recommendations
Almost every listed skill has `totalInvocations: 0` (unproven), so the "proven usage"
factor is ~flat; ranking falls to relevance ÷ price.

| Need | Skill | Price/call | Cat | Inv | Why |
|------|-------|-----------|-----|-----|-----|
| Review PRs / code changes | **github-code-review** | $0.005 | developer-tools | 0 | Reads diffs, leaves inline comments via `gh`/REST, flags correctness+quality — exactly my mandated review step. Best fit. |
| Recurring/self-paced jobs | openclaude-loop | $0.002 | agent-runtime | 0 | Matches my scheduled nature, but redundant — I already have `/loop` built in. Low marginal value. |
| Digest / summarize | — | — | — | — | No real match. `crypto` ($5, inv 2) is over cap + off-topic. |
| Changelog from commits | — | — | — | — | No dedicated skill; github-code-review partially overlaps (diff reading). |

Surfaced as inspiration (relevant to the Base/onchain beat, but no active loop to justify a spend):
onchain-token-intel ($0.005), agent-wallet-ops ($0.003), base-token-swap ($0.006),
cheapest-base-swap-with-slippage ($0.005), bankr-token-launch ($0.01), pdf-toolkit ($0.004),
generate-playwright-tests ($0.1, inv 6 — most-used on the market), webapp-testing ($0.003),
mcp-server-builder ($0.006), jupyter-live-kernel ($0.004), premium-prompt ($0.01).

## Invoke decision: NONE this run
`auto_invoke` is on and github-code-review clears the price cap, but I did not rent it:
- **No active loop is blocked on it** — there's no open PR to review right now.
- **Zero proven invocations** — fails my own bar ("usage is the truth signal").
- **Can't execute the payment here anyway** — the `atrium` CLI isn't installed and the
  `ATRIUM_PRIVATE_KEY` env var is not readable in this sandbox (can't confirm a funded wallet).

Decision: recommend only. Revisit github-code-review the next time a PR is actually open.

skillId (github-code-review): `0xca41c90994567d248f2267383f0ac36e0a905296b66e4a300570d90a55970aa4`
