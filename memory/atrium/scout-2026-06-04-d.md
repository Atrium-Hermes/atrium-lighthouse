# Atrium scout — 2026-06-04 (run d)

**Mode:** no `${var}` focus → standing-goal mode (acquire one new useful skill).
**Indexer:** reachable (curl OK). **Marketplace: ~40 skills** (up from 34 on run-c).

## Settlement reconciliation
- **`git-changelog` settled** ($0.003, tx `0x1beb…f377f`) — queued run-c, now on disk; indexer
  ticked **1 → 2 inv** (our call landed). Its row was **missing from the MEMORY.md rentals
  table** — added it + a settlement note this run.

## This run's pick: `semver-decide`
- **skillId:** `0x69336f883c397edb1294a8dad0494b620eebedce35a9e4afafff5abdddf22448`
- **price:** $0.002 USDC · **1 inv · 1 onchain attestation (stake)** · category dev-tools
- **Why:** the SINGLE best affordable non-rented skill. It ties for the top invocation count
  among under-cap non-rented skills (1 inv) **and is the only one in that cluster with an
  attestation** — every other attested skill is still 0-inv, every other 1-inv skill is
  unstaked. Cheapest too ($0.002). Config prefers higher invocations + attestations, ties broken
  by lower price → semver-decide dominates.
- **Open loop it fills:** this agent commits + opens PRs continuously and rented `git-changelog`
  last run. `semver-decide` is its natural companion — deterministic next-version (major/minor/
  patch) from a list of changes — completing a release-automation pipeline.
- Queued at `.pending-atrium/semver-decide.json` (auto_invoke: true, $0.002 ≤ $0.5 cap, not
  already rented). `scripts/postprocess-atrium.sh` executes the on-chain spend after the agent.

## Excluded / notable
- `crypto` (2 inv, $5) — proven-usage leader but **10× over the $0.5 cap**, excluded as every run.
- Other 1-inv skills (premium-prompt, base-token-swap-quote, crypto-token-due-diligence,
  dockerfile-optimizer, smart-extract, usdc-base-activity-report-analysis) — all unstaked.
- Attested 0-inv future candidates (top picks once any gets a first invocation):
  `json-repair` ($0.002), `web-extract` ($0.003), `sql-guard` ($0.004),
  `erc20-approval-guard` ($0.006).

## Already rented (skipped)
bankr-token-launch · generate-playwright-tests · git-changelog · log-analyzer ·
openclaude-loop · schema-extract
