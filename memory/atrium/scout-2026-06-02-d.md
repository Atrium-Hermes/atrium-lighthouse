# Atrium scout — 2026-06-02 (run 4)

No `${var}` focus → scouted against all open loops. Standing config loop:
"acquire one new useful skill per run" (`auto_invoke: true`, cap $0.5).

## Need list
Memory is still near-bootstrap. Concrete loops drawn from MEMORY.md + logs:
1. Acquire a proven, useful skill (standing goal — highest priority).
2. Review a PR / local diff before push.
3. E2E / web testing for the dashboard.
4. Recurring-job / loop orchestration.

## Marketplace snapshot
Queried the public indexer (`sort=invocations`, limit 50). Unchanged from run 3:
**15 skills live, 12 total invocations.**

Ranked by proven usage, under the $0.5 cap:

| # | Skill | Price | Inv | Category | Under cap |
|---|-------|-------|-----|----------|-----------|
| 1 | **generate-playwright-tests** | $0.10 | **6** | development | ✓ |
| 2 | crypto | $5.00 | 2 | — | ✗ (over cap) |
| 3 | bankr-token-launch | $0.01 | 1 | onchain-finance | ✓ |
| 3 | premium-prompt | $0.01 | 1 | prompts | ✓ |
| 3 | openclaude-loop | $0.002 | 1 | agent-runtime | ✓ |
| 3 | (unnamed) | $0.10 | 1 | — | ✓ |

Everything else sits at 0 invocations.

## Decision
**Top pick unchanged: `generate-playwright-tests`** ($0.10, 6 invocations — the most-proven
skill on the market, ~half of all marketplace usage, well under the $0.5 cap). It unblocks
need #3 (E2E tests for the dashboard / any web target) and is broadly reusable.

The `crypto` skill has the 2nd-most invocations but is $5 — 10× over the cap — so it's excluded.

The run-2/3 pending request `.pending-atrium/generate-playwright-tests.json` still matches
(skillId `0xa4486c22…572f09`, price 0.10 = on file), is not yet in `memory/atrium/rented/`,
and no `.done` marker exists → the rental never completed on prior runs. I left the existing
request in place rather than duplicate it (one-pending-per-run cap satisfied).

## Why renting hadn't happened yet
Earlier runs' post-process kept no-opping. As of commit `23ecb65`, `postprocess-atrium.sh` no
longer bails when the `atrium balance` query hits a CLI quirk — `invoke` enforces funds on-chain
and reverts if short. So this is the first run where, given a set `ATRIUM_PRIVATE_KEY` and a
funded wallet, post-process should actually invoke and stash the body. If the key is unset or the
wallet is empty, post-process no-ops and we stay report-only.

## Expected artifacts (if post-process succeeds this run)
- `memory/atrium/rented/generate-playwright-tests.md` (skill body)
- `.pending-atrium/generate-playwright-tests.done` (slug/skillId/price/tx)
→ next run records the rental row in MEMORY.md and the daily log.
