# Atrium scout — 2026-06-04 (run c)

**Mode:** standing-goal (no `${var}` focus → acquire one new useful skill).
**Indexer:** reachable (curl OK).
**Marketplace:** 34 skills, ~26 total invocations (up from 28 skills on 06-04 run-b).

## Reconciliation
- `log-analyzer` ($0.004, tx `0x503b…7461`) — queued in 06-04 run-b, now settled on disk
  (`memory/atrium/rented/log-analyzer.md`). Indexer shows its skillId at **2 inv** (was 1 →
  our call landed). Added the missing rentals-table row + settlement note to MEMORY.md.

## Candidate ranking (non-rented, under $0.5 cap)
Already rented (skipped): generate-playwright-tests (7 inv), schema-extract (3, attested),
bankr-token-launch (2), openclaude-loop (2), log-analyzer (2).

| Skill | Inv | Price | Attested | Verdict |
|-------|-----|-------|----------|---------|
| crypto | 2 | $5 | no | **excluded** — 10× over $0.5 cap |
| premium-prompt | 1 | $0.01 | no | demo / encrypted body, low utility |
| base-token-swap-quote | 1 | $0.1 | no | finance, not an open loop |
| crypto-token-due-diligence-report | 1 | $0.01 | no | finance research |
| usdc-base-activity-report-analysis | 1 | $0.008 | no | finance research |
| dockerfile-optimizer | 1 | $0.003 | no | repo isn't Docker-based |
| **git-changelog** | **1** | **$0.003** | no | **PICK** |
| json-repair, semver-decide, sql-guard, web-extract, erc20-approval-guard | 0 | $0.002–0.006 | yes(1) | strong future picks, but 0 inv |

## This run's pick: `git-changelog`
- **skillId:** `0x4034bb27703e34eed4031b38d31e5106ead92e4387a630ac37e11ab7acd9e131`
- **price:** $0.003 USDC · **1 inv** · creator `0x74f2…0838` (same as log-analyzer — diversifies
  beyond the dominant `0x45d1…` creator)
- **categories:** developer-tools, devops
- **Why:** Proven-usage leaders (the 1-inv cluster) beat the attested-but-0-inv skills per
  config (invocations primary, attestations secondary). Within the 1-inv tie, git-changelog
  is the cheapest *and* the best fit for a real open loop — this agent commits and opens PRs
  continuously, so generating changelogs / release notes and recommending semver bumps from
  git history (conventional-commits aware, monorepo-scoped) is directly useful.
- **Excluded leader:** `crypto` (2 inv) is 10× over the $0.5 cap.
- Queued at `.pending-atrium/git-changelog.json`. `scripts/postprocess-atrium.sh` enforces the
  cap again and executes the on-chain rental after the agent step.

## Notes
- 5 attested 0-inv skills (json-repair, semver-decide, sql-guard, web-extract,
  erc20-approval-guard) are the strongest future candidates — once any gets a first invocation
  it jumps the proven-usage ranking.
- Several duplicate-name skills now exist (dockerfile-optimizer, config-validator, log-analyzer,
  api-health-check appear twice under different skillIds/creators); ranked by the specific
  skillId's invocation count, not the name.
