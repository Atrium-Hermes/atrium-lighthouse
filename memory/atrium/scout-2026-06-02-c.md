# Atrium scout — 2026-06-02 (run 3)

Mode: normal (thin need list → standing "acquire one useful skill" loop).
Indexer: https://indexer-production-92e5.up.railway.app (read-only, no key).
Marketplace size: **15 skills live**, **12 total invocations** across all (unchanged vs run 2).
Config: `auto_invoke: true`, `max_price_usdc: 0.5`, allowed = any.

## What changed vs run 2
- The wallet key is now wired in: `aeon.yml:807` passes
  `ATRIUM_PRIVATE_KEY: ${{ secrets.ATRIUM_PRIVATE_KEY }}` into the **"Run post-process
  scripts"** step (commit `c25252b`). The PR from run 2 is merged.
- So the rental that was *queued* last run can finally execute: `scripts/postprocess-atrium.sh`
  runs after this agent step, re-checks the cap, and invokes the skill (if the secret is
  set + the wallet is funded). The key never touches the Claude step.

## Need list (memory still thin)
1. Review PRs / code changes before pushing (CLAUDE.md mandate).
2. Test the dashboard UI / web targets.
3. Digest / summarize content.
4. Recurring / self-paced jobs (covered by built-in `/loop`).

No specific `${var}` focus → standing loop: acquire the single highest-value skill not
already rented.

## Ranked recommendations (relevance × proven usage ÷ price)
Proven usage barely separates the field — only two skills have meaningful invocations
under the cap, and one (`crypto`, $5) is over it.

| Need | Skill | Price | Inv | Why |
|------|-------|-------|-----|-----|
| **Acquire highest-value capability** | **generate-playwright-tests** | $0.1 | **6** | Most-invoked skill on the market (half of all 12 invocations), $0.585 earned. Generates Playwright E2E tests from a URL + spec — directly usable to test `dashboard/`. Under the $0.5 cap. **Top pick (unchanged).** |
| Test dashboard UI | webapp-testing | $0.003 | 0 | Drives a local app with Playwright; cheap but unproven. |
| Review PRs | github-code-review | $0.005 | 0 | Matches the review mandate; unproven. |
| Parse documents | pdf-toolkit | $0.004 | 0 | Useful general capability; unproven. |
| Build MCP tools | mcp-server-builder | $0.006 | 0 | For extending the agent's own tools. |

Over cap / skipped: `crypto` ($5, inv 2 — over cap), one unnamed skill ($0.1, no
name/desc — can't evaluate). Onchain-finance cluster (base-token-swap,
onchain-token-intel, agent-wallet-ops, cheapest-base-swap, bankr-token-launch) is
on-theme but no active trading loop justifies a spend.

## Invoke decision
`auto_invoke: true`, top pick clears the cap, not in `memory/atrium/rented/`. A valid
rental request already sits at `.pending-atrium/generate-playwright-tests.json`
(skillId `0xa448…2f09`, $0.1) from run 2. It still matches the top pick, so I left it in
place rather than duplicating it — the one-pending-per-run cap is satisfied.

This is the first run with the key actually wired, so post-process should now perform the
on-chain `atrium invoke`, stash the body under
`memory/atrium/rented/generate-playwright-tests.md`, and drop a `.done` marker for the
next run to log the tx. If the secret still isn't set or the wallet is unfunded,
post-process no-ops safely and we stay report-only.

skillId (generate-playwright-tests): `0xa4486c22d45af49913ebca595234e8897e8f22bbb6c7e348fb1e424de9572f09`
