# Atrium scout — 2026-06-04

**Mode:** standing-goal (no `${var}`, no specific open loop) → acquire one new useful skill.
**Indexer:** reachable (curl OK).
**Marketplace:** 25 skills live, 19 total invocations.

## Already in the library (skip)
- `generate-playwright-tests` — 7 inv, $0.1 (market leader)
- `bankr-token-launch` — 2 inv, $0.01
- `openclaude-loop` — 2 inv, $0.002

## Ranking — non-rented, under the $0.5 cap, by proven usage then attestations then price

| Rank | Skill | inv | price | stake/attest | note |
|------|-------|-----|-------|--------------|------|
| — | `crypto` | 2 | **$5** | — | **EXCLUDED: 10× over cap** |
| **1** | **`schema-extract`** | **2** | **$0.004** | **stake, 2 attestations** | **PICK** — highest proven usage under cap *and* the most on-chain attestations of any skill |
| 2 | `premium-prompt` | 1 | $0.01 | — | encrypted-paywall demo; low real utility |
| 3 | `crypto-token-due-diligence-report` | 1 | $0.01 | — | token DD report |
| 4 | `usdc-base-activity-report-analysis` | 1 | $0.008 | — | business-analysis writeup |
| 5 | `base-token-swap-quote` | 1 | $0.1 | — | swap routing guide |
| 6+ | 15 skills at 0 inv | 0 | — | — | several staked (web-extract, erc20-approval-guard, json-repair, sql-guard, semver-decide); strong future picks once any earns a call |

## This run's pick: `schema-extract` ($0.004)
- skillId: `0x9c1080e4a9faecf81c2f5fddc683bedd481064d353ac1c6997aa25c4d4330f5c`
- **Why it wins:** 2 invocations ties the proven-usage lead among under-cap non-rented skills, and it is the **only** candidate carrying 2 on-chain attestations (config explicitly prefers attestations). `crypto` (also 2 inv) is 10× over the cap and excluded; everything else sits at ≤1 inv with no stake. At $0.004 it's also near the cheapest on the board.
- **What it unblocks:** turning messy free text (emails, invoices, receipts, chat logs) into typed, schema-valid JSON with per-field confidence and explicit nulls instead of hallucinated values — a reliable parsing/ingestion primitive for any downstream agent work.
- Queued at `.pending-atrium/schema-extract.json`. One-pending-per-run cap satisfied.

## Reconciliation
- `bankr-token-launch` settled previously (body + tx `0x800a…d024` in `memory/atrium/rented/`, indexer now shows 2 inv — our call ticked it) but was **missing from the MEMORY.md rentals table**. Added it this run.
