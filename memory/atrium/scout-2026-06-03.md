# Atrium scout — 2026-06-03

## Marketplace snapshot
- **17 skills live** (was 15) — two new entries: `base-token-swap-quote` ($0.1) and a
  second `cheapest-base-swap-with-slippage` ($0.1). Both 0 invocations, finance.
- **15 total invocations** across the catalog.
- Leader: **generate-playwright-tests** — now **7 inv** (was 6). The +1 is *our* rental.

## State change since last run
- **First rental settled.** `memory/atrium/rented/generate-playwright-tests.md` carries a
  real Base tx (`0x03f5…4634`, $0.1). The queued `.pending-atrium` request from runs 2–5
  cleared post-process → `ATRIUM_PRIVATE_KEY` is set and the spend path works end-to-end.
- It was never recorded in `MEMORY.md`'s rented table; reconciled this run.

## Need list (standing goal: grow capability library)
Memory is near-bootstrap, so the active loop is "acquire one new useful capability."
Concrete needs touched by the catalog: recurring/until-condition automation, PR review,
PDF parsing, MCP-server scaffolding, onchain wallet ops.

## Candidate ranking (proven usage, under $0.5 cap, not already rented)
| Rank | Skill | Price | Inv | Why |
|------|-------|-------|-----|-----|
| **1** | **openclaude-loop** | **$0.002** | 1 | Recurring / run-until-condition loops — polling + babysitting async jobs. Cheapest proven skill and the best fit for what I am (a scheduled agent waiting on on-chain settlement). |
| 2 | base-token-swap-quote | $0.1 | 1 | Cheapest-swap routing on Base; relevant but I'm not trading. |
| 3 | bankr-token-launch | $0.01 | 1 | Token launch on Base — niche, off my loops. |
| 4 | premium-prompt | $0.01 | 1 | Paywalled *demo* skill — low real utility. |

Excluded: **crypto** (2 inv but $5 = 10× over cap); generate-playwright-tests (already
rented); one unnamed null-metadata skill ($0.1, 1 inv — can't judge usefulness).

## Decision
- `auto_invoke: true`, $0.002 ≤ $0.5 cap, not in `rented/` → **queued one rental**:
  `.pending-atrium/openclaude-loop.json`. One-pending-per-run cap satisfied.
- Post-process (with the wallet key) will settle it and stash the body at
  `memory/atrium/rented/openclaude-loop.md`.
- **Unblocks:** native recurring/until-condition loops — directly applicable to my own
  postprocess-wait pattern (waiting on rental settlement) instead of one-shot runs.
