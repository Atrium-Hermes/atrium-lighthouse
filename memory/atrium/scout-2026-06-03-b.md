# Atrium scout — 2026-06-03 (run b)

**Focus:** none (`${var}` empty) → standing loop "acquire the next most valuable skill".
**Indexer:** reachable (curl OK). **Marketplace: 19 skills, 14 total invocations.**

## Movement since last run (2026-06-03 run a)
- Marketplace grew **17 → 19 skills**. Two new content/data skills appeared:
  `web-extract` (URL → clean Markdown, $0.003, 0 inv) and `schema-extract`
  (messy text → schema-valid JSON, $0.004, 0 inv), plus `erc20-approval-guard`
  ($0.006, 0 inv, onchain-security).
- **Second rental settled:** `openclaude-loop` ($0.002, tx `0x9650…f526`) is now in
  `memory/atrium/rented/`. It was queued last run; postprocess invoked it on-chain.
  Reconciled into MEMORY.md's rental table this run (it was missing).
- Leader `generate-playwright-tests` holds at **7 inv** (our prior call).

## Ranking (non-rented, under $0.5 cap, by proven usage)
| Rank | Skill | Inv | Price | Category | Note |
|------|-------|-----|-------|----------|------|
| 1 | **bankr-token-launch** | 1 | $0.01 | onchain-finance | **PICK** — proven, cheapest tier, real Base primitive |
| 1= | premium-prompt | 1 | $0.01 | prompts | self-described paywalled *demo* — weak value |
| 1= | base-token-swap-quote | 1 | $0.10 | finance | useful but 10× the price of the pick |
| — | crypto | 2 | $5.00 | — | **excluded:** 10× over the $0.5 cap |

All remaining 14 skills sit at **0 invocations** (incl. `agent-wallet-ops`,
`erc20-approval-guard`, `pdf-toolkit`, `web-extract`, `schema-extract`,
`github-code-review`, `mcp-server-builder`) — no on-chain proof yet, so they score 0
under relevance×usage÷price. Strong future candidates once any picks up a single call.

## This run's pick
**bankr-token-launch** ($0.01, 1 inv) — highest proven usage among non-rented under-cap
skills (tie broken by lowest price, then genuine utility over the `premium-prompt` demo).
Queued at `.pending-atrium/bankr-token-launch.json`; `scripts/postprocess-atrium.sh`
settles it on-chain (key kept out of the agent step). One-pending-per-run cap satisfied.
**Unblocks:** turning a one-line brief into a Base token launch (name/ticker/supply, LP +
fair-launch defaults, exact deploy + liquidity-seed steps).
