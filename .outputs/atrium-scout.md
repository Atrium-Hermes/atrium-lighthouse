Atrium scout (run 5): marketplace unchanged — 15 skills live, leader still at 6 invocations. Top pick stays generate-playwright-tests ($0.10, 6 inv, under the $0.50 cap); crypto is the only other proven skill and sits 10x over cap. Report-only again — rented/ empty and no .done marker after 4 settle-capable runs, so ATRIUM_PRIVATE_KEY is still unset. The pick is already queued at .pending-atrium/; set the secret on a funded Base wallet and the next run rents it automatically.
