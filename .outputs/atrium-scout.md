Atrium scout — 15 skills live, ~12 invocations total. Top pick: generate-playwright-tests ($0.1, 6 invocations — the most-used skill on the market, under the $0.5 cap). Rented nothing: ran `atrium balance` and the wallet key isn't in the env — ATRIUM_PRIVATE_KEY is passed to no workflow step, so renting has never been possible. Pushed branch scout/wire-atrium-invoke: a secure postprocess-atrium.sh that spends with the key kept out of the agent step. To go live: set the ATRIUM_PRIVATE_KEY secret, add one env line to aeon.yml (bot can't edit workflows), open + merge the PR: https://github.com/Atrium-Hermes/atrium-lighthouse/compare/main...scout/wire-atrium-invoke?expand=1 — next run rents automatically.
